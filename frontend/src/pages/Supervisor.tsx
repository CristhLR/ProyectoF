import { Link } from 'react-router-dom';

const Supervisor = () => {
  return (
    <main>
      <h1>Supervisor</h1>
      <nav>
        <Link to="/login">Ir a Login</Link>
      </nav>
    </main>
  );
};

export default Supervisor;
